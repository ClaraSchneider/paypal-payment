{
  "name": "paypal-payment",
  "version": "1.0.0",
  "description": "A project for handling PayPal payments",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "echo \"Build completed\"",
    "deploy": "netlify deploy --prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@paypal/checkout-server-sdk": "^1.0.3",
    "express": "^5.1.0",
    "body-parser": "^1.19.0"
  },
  "devDependencies": {
    "netlify-cli": "^2.59.0"
  }
}